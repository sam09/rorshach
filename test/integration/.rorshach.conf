CREATE	*	echo "CREATED ${FULLPATH}" >> ${BASEDIR}/../output
MODIFY	*.cpp	g++ ${FULLPATH} -o ${BASEDIR}/test
DELETE	*	echo "DELETED ${FULLPATH}" >> ${BASEDIR}/../output
RENAME	*	echo "RENAMED ${FULLPATH} ${NEWFULLPATH}" >> ${BASEDIR}/../output
